import { test } from '@playwright/test';

// Uncomment this import and the test will fail
// even though all this file imports is testData
// import {testData} from '../mockData'

test('Happy Path', async () => {
    console.log('test running')
});

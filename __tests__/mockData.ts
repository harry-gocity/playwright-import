// We could have these defined as we use them in our mock redux state
import { QueryStatus } from '@reduxjs/toolkit/query';
import { QueryCacheKey } from '@reduxjs/toolkit/src/query/core/apiState';


// either way, they shouldn't be important to playwright, all I export is testData
export const testData = {}